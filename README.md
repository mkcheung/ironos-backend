# ironos-backend

Django 5.2 backend for IronOS — AI fitness coaching platform.

## Quick start

```bash
cp .env.example .env
docker compose up --build
```

Health check: `GET http://localhost:8000/api/health/`
